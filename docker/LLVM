FROM ubuntu:latest as llvm
WORKDIR /
RUN apt update && apt install -qq -y build-essential wget gpg software-properties-common gnupg && apt-get clean all

RUN wget https://apt.llvm.org/llvm.sh
RUN chmod +x llvm.sh
RUN /llvm.sh 16
ENTRYPOINT [ "/bin/sh" ]